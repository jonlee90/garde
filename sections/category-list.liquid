<div class="category-list section">
  <div class="category-container">
    {% for block in section.blocks %}
      <a class="category-link" href="{{ block.settings.collection.url | img_url }}">
        <img src="{{ block.settings.image | img_url: 'master' }}" alt="{{ block.settings.title }}">
        <span>
          {% if block.settings.title == '' %} 
            {{ block.settings.collection.title }}
          {% else %}
            {{ block.settings.title }}
          {% endif %}
        </span>
      </a>
    {% endfor %}
  </div>  
</div>

{% schema %}
{
  "name": "Category List",
  "max_blocks": 4,
  "blocks": [
    {
      "name": "Categories",
      "type": "category",
      "settings": [
        {
          "type": "collection",
          "id": "collection",
          "label": "Category"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Category Image"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "info": "Optional"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Category List",
      "category": "Image",
      "blocks": [
        {
          "type": "category"
        },
        {
          "type": "category"
        },
        {
          "type": "category"
        },
        {
          "type": "category"
        }
      ]
    }
  ]
}
{% endschema %}